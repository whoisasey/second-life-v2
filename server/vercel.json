{
	"version": 2,
	"builds": [
		{
			"src": "server/index.js",
			"use": "@now/node"
		}
	],
	"routes": [
		{
			"src": "server/(.*)",
			"dest": "server/index.js",
			"methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
		}
	]
}
